research_id: amd_current_strategy_replay_train_plumbing
ticker: AMD
research_mode: CURRENT_STRATEGY_REPLAY
hypothesis: Plumbing validation of the existing strategy rule set.
population_source: {type: ticker_daily_calendar, frozen: false}
signal_definition: {creates_new_entry_dates: true, purpose: current_strategy_replay}
entry_date_rule: {rule: current_strategy_entry_date}
date_range: {split: TRAIN}
split_policy: {name: TRAIN_ONLY}
contract_selection_policy: {mode: RULE_SET}
lifecycle_policy: {source: canonical_lifecycle_adapter, benchmark_symbol: QQQ, outcome_horizon_policy: SPLIT_CUTOFF}
frozen_parameters: {}
allowed_parameters: {rules: true}
final_oos_access: false
production_changes_allowed: false
rules:
  dte_min: 30
  dte_max: 45
  safe_strike_atr: 2.3
  allowed_widths: [5, 10, 2]
  width_mode: ALL
  min_credit_width_ratio: 0.10
  trend_gate: true
  pullback_gate: true
  support_gate: true
  regime_gate: false
  event_gate: true
  liquidity_gate: true
  predictability_gate: true
