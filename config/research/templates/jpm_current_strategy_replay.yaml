research_id: jpm_current_strategy_replay_from_2018_20260825
ticker: JPM
strategy_type: PUT_CREDIT_SPREAD
research_mode: CURRENT_STRATEGY_REPLAY
hypothesis: Plumbing validation of the existing strategy rule set using the active canonical logical options route from the declared executable data boundary.
population_source: {type: ticker_daily_calendar, frozen: false}
signal_definition: {creates_new_entry_dates: true, purpose: current_strategy_replay, benchmark_symbol: QQQ}
entry_date_rule: {rule: current_strategy_entry_date}
date_range: {start: '2018-01-01', end: '2025-12-31', split: TRAIN}
split_policy: {name: TRAIN_ONLY, train_end: '2025-12-31'}
contract_selection_policy: {mode: RULE_SET}
lifecycle_policy: {source: canonical_lifecycle_adapter}
frozen_parameters: {}
allowed_parameters: {rules: true}
final_oos_access: false
production_changes_allowed: false
rules:
  dte_min: 30
  dte_max: 45
  safe_strike_atr: 2.3
  allowed_widths: [5, 10, 2]
  width_mode: ALL
  min_credit_width_ratio: 0.10
  trend_gate: true
  pullback_gate: true
  support_gate: true
  regime_gate: false
  event_gate: true
  liquidity_gate: true
  predictability_gate: true
