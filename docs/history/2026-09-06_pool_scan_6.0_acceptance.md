# Pool Scan 6.0 acceptance — 2026-09-06

Status: full queue processing acceptance PASS. This is not production trading
approval or a promotion of the frozen recovery baseline to stable.

## Version, invocation and evidence

- Branch/worktree: `6.0`, `H:/workspace/PCSOS-6.0`.
- Accepted source: `ad7fb45e40dad10f18e613c8118823d79196a24b`.
- Tag: `pool-scan-6.0-acceptance-20260906`.
- Run: `384187f5a6e0465e968d01a5983bb017`.
- Clean committed source, EOD / READ_ONLY, session `2026-09-04`, requested
  as-of `2026-09-04T16:01:00-04:00`, `global_pcs_candidates`, 8 workers.
- Stage/global budgets stayed at 900/1800 seconds. No budget increase.
- Data/config cwd: `H:/workspace/PCSOS`.
- Manifest snapshot: `5af72f892609519c0293b922f8515b3abdeb14683b904e7922a30501683c6400`.
- Evidence root: `H:/workspace/PCSOS/pool_scan_runs/6.0_release_acceptance`.
  `invocation.json` contains exact argv, source SHA, timestamps and config/manifest
  hashes; `config_snapshot/` retains YAML inputs; `python_environment.json`
  records the interpreter and dependency versions.
- Full output: `stdout.json`; operation events: `stderr.log`.
- Final run files: `384187f5a6e0465e968d01a5983bb017/` below the evidence root.
- Checkpoint: `.checkpoints/06e6b7afbcea04486e8f903828989efc1c3f0350c2e5b477d6b19cffa7f6400e.json`.
- Independent checks: `acceptance_validation.json` and `equivalence.json`.
- [Copyable new/resume commands](../pool_scan_6.0.md).

## Complete-pool result

| Metric | Count |
|---|---:|
| raw_count | 2953 |
| daily_ready | 2017 |
| PCS_eligible | 2017 |
| timing_watch | 409 |
| timing_entry_ready | 38 |
| options_check | 38 |
| spread_count | 0 |
| pcs_trade_ready | 0 |
| execution failures | 0 |
| timeouts | 0 |
| unprocessed | 0 |
| blocked assessments | 977 |
| checkpoint hits in the fresh full run | 0 |

All 2953 requested symbols are unique, in configured order, and have an explicit
result. Row-derived counts equal the summary. All 16 manifested output payload
hashes verify. Canonical manifest and config hashes were unchanged after execution.

Coverage: 2017 daily-ready symbols; 2014 have usable timing evidence. Daily
blockers are 455 `ACTIVE_GENERATION_MISSING` and 481 `DAILY_STALE`. FISV, TBIL and
TFLO retain `TIMING_EVIDENCE_UNAVAILABLE`. All 38 timing candidates are blocked
at options with the existing route reason
`DATA_NOT_INGESTED_OR_CANONICAL_MANIFEST_MISSING`. This does not establish that
an external provider has no data. There are 1976 other evaluated results and 977
overall blocked assessments; blockers are not strategy rejects. The existing
`rejected_count` is 1035 and is a distinct subset/state.

The canonical data path really executed: 2017 verified daily frame reads and
2060 handle resolution attempts, including failed readiness/options attempts.
Options frames read: 0. Provider, promotion and recovery calls: 0.

## Measured cost and changes

End-to-end wall time: **403.638 seconds**, exit code 0. Runner wall time is
398.766 seconds: readiness audit 97.283 seconds, benchmark 0.034 seconds,
scan 301.348 seconds, validation 0.015 seconds. Preparation/final adapters were
not run. Startup and final serialization explain the remaining wall time.

Operation totals are cumulative worker time, not additive wall-clock stages:
manifest preflight 13.685 s; daily handle locate/verify 82.204 s; verified daily
read 99.623 s; indicators 67.298 s; interpretation/scoring/trend/timing gates
0.716 s; options locate/verify 8.236 s; checkpoint saves 40.669 s.

The preceding complete run at f79c604 took 777.527 seconds, with 408.870 seconds
in checkpoint saves. The accepted run is 48.1% faster end to end. All 2953
business result rows and all summary fields are identical between these runs,
excluding run identifiers and measured latency fields. Both used identical
configuration and canonical manifest hashes. Prior evidence remains under
`H:/workspace/PCSOS/pool_scan_runs/6.0_final_acceptance`.

Actual changed files and purposes:

- `src/pcs/data/access.py`, `src/pcs/data/strategy_readiness.py`,
  `src/pcs/pool/runtime.py`: index the run's manifest by dataset/symbol, pass the
  matching snapshot through verified reads, and retain generation/checksum,
  fingerprint, file-change and routed-manifest checks. Add operation telemetry.
- `src/pcs/pool/runner.py`: stop startup from erasing checkpoints; bind exact
  universe/config/code and per-row daily/benchmark/options identity; save timing
  before options; resume compatible stages; reject failed/incompatible cached
  rows; serialize only replaced checkpoint rows; separate coverage counters.
- `src/pcs/pool/concurrency.py`, `src/pcs/pool/models.py`: serial outcome callback
  and additive stage/cache fields. Late worker callbacks cannot overwrite saved
  state after collection ends.
- `src/pcs/pool/process.py`, `src/pcs/cli.py`: explicit new/exact-resume modes,
  preserve validated partial results after process termination, and do not treat
  startup's unverified old cache as current assessments.
- `src/pcs/pool/artifacts.py`: serialize new maps safely to Parquet and prevent
  PARTIAL artifacts being published as CURRENT.
- Focused tests: checkpoint resume, snapshot reuse, process and artifact tests.
- Documentation: pool contract and usage/evidence guides. The AGENTS.md gate and
  pre-existing Pool Scan working changes were carried into the separate branch;
  this task did not edit the original 5.6 worktree's source. Additional changes
  observed there during this task were left untouched.

`git diff c62db50 ad7fb45 -- src/pcs/trend src/pcs/entry config/pcs_rules.yaml`
is empty: no strategy implementation or trading rules changed.

## Focused validation

The initial applicable contract selection passed **67 tests** across checkpoint,
snapshot/runtime, concurrency/process, verified-boundary, final-correctness,
artifact and runner-contract tests. After the PARTIAL/telemetry fix the affected
24 tests passed. The final changed checkpoint/process/artifact selection passed
**21 tests** with:

```text
C:/Python313/python.exe -m pytest tests/pool/test_checkpoint_resume.py tests/pool/test_process.py tests/pool/test_artifacts.py -q
```

These are overlapping selections, not cumulative test totals. A full repository
suite or research replay was not run.

Additional evidence at `H:/workspace/PCSOS/pool_scan_runs/6.0_checks`:

- `reuse_validation.json`: real canonical six-symbol fresh/resume/new-run checks
  on the accepted source. Business results match, exact resume retains run id,
  new-run changes it, both reuse all six assessments, and only the benchmark is
  reread as a verified frame. Indicators are not recomputed.
- `one_timeout/validation.json`: isolated three-symbol fixture, one timeout,
  both other symbols complete, zero unprocessed, late checkpoint write prevented.
- `timeout_fixture/validation.json`: process-deadline fixture retains the saved
  row, distinguishes a running timeout from unstarted work, leaves no child
  process, and writes non-CURRENT partial artifacts.
- `checkpoint_performance.json`: 15 saves of 2953 real result rows, 2.580 s before
  versus 0.431 s after. Parsed checkpoint JSON is identical except updated_at.
- Unit fixtures cover interrupted timing-stage continuation, per-symbol data
  invalidation, shared benchmark invalidation, wrong manifest routing, mutated
  canonical partitions, stricter warmup and physical checksum preservation.

One pre-existing test remains failing:
`tests/data/test_pool_options_handle_routing.py::test_timing_ready_verified_options_produces_one_spread`.
It also failed on the original 5.6 source; its mocked trend does not satisfy the
current timing producer contract. No strategy change was made to satisfy it.
The current verified-boundary positive spread and negative fixtures passed;
those are fixture evidence, not real options availability in this full scan.

An earlier candidate attempt at c47a2cc exited with 4294967295 and no final JSON;
it is not accepted. Its saved rows and logs remain under `6.0_acceptance`.
No Python traceback or matching Windows application crash record was found;
the cause was not established. The two subsequent independently launched full
runs completed. Neither historical 048dc79 output nor the earlier timeout was
used as current readiness evidence. Compatible metadata reuse code/tests were
adapted from dbee757 and validated against the present source and data.

Remaining execution boundaries are documented in the usage guide: API thread
deadlines bound collection; CLI read-only process supervision supplies the hard
stop. Code/config invalidation is conservative, while ticker/options input
changes have per-row/stage reuse. Missing options/event/portfolio inputs do not
become trade-ready results.
