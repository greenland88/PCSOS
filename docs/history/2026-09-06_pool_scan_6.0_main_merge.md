# Pool Scan 6.0 main integration

The user authorized merging the accepted 6.0 release into main and pushing main,
6.0 and the acceptance tag, without force pushes or another full-pool scan.

## Scope

- Remote main before integration: `048dc79cd166df5c6aa64870557f3acaf4e6eadc`.
- Accepted source: `ad7fb45e40dad10f18e613c8118823d79196a24b`.
- Acceptance tag: `pool-scan-6.0-acceptance-20260906` (unchanged).
- 6.0 head: `b9ee082da733b43c7da76108c3a2e294a58c34b0`; changes after the
  accepted source are documentation only.
- Common ancestor: `c62db50412cf9d74333863a360966c4774804874`.
  Main had 15 independent commits and 6.0 had four; a merge was required.
- Integration worktree: `H:/workspace/PCSOS-main-6.0-integration`.
  Existing source/data/research worktrees were not edited or reset.

## Conflict resolution

Preserved main's decision-context adapters, options preparation orchestration,
verified quote continuation, candidate recovery, and saved AI evidence. Integrated
6.0's full-queue checkpoints, bounded process recovery, input-bound cache and
operation telemetry. The CLI accepts both decision-context and resume controls.
All three nested result maps retain safe Parquet serialization; partial artifacts
remain non-current.

The bounded readiness audit now shares the verified runtime. Authorized preparation
refreshes manifest and routed identity caches. Candidate continuation writes its
latest options result into the queue checkpoint, and final checkpoint rows retain
final gate outcomes. Timing-only recovery retains the original evidence and rule
context. Late scan workers cannot write progress after result collection ends.
Explicit no-resume also disables candidate timing reuse; identity-validated
candidate timing reuse remains available when injected decision callbacks require
fresh final decisions.

No changes to `src/pcs/trend`, `src/pcs/entry`, or `config` relative to main.

## Final validation

From the integration worktree, PowerShell:

```powershell
$env:PYTHONPATH='H:/workspace/PCSOS-main-6.0-integration/src'
C:/Python313/python.exe -m pytest tests/pool/test_checkpoint_resume.py tests/pool/test_daily_snapshot_reuse.py tests/pool/test_process.py tests/pool/test_artifacts.py tests/pool/test_context_adapters.py tests/pool/test_on_demand_recovery.py tests/pool/test_runtime_performance.py tests/pool/test_final_correctness.py tests/pool/test_preparation_orchestration.py tests/pool/test_verified_boundary.py tests/pool/test_runner_contract.py tests/pool/test_concurrency.py -q --tb=short
```

**140 passed in 44.50 seconds** on the final merged Python source. Log:
`H:/workspace/PCSOS-main-6.0-merge-final-tests.log`. `git diff --check` passed.
New integration coverage verifies final options checkpoint contents, fresh run IDs,
compatible full-queue reuse, options generation invalidation without timing
recomputation, and preservation of saved timing evidence.

Earlier integration attempts exposed routed-cache invalidation and timing-evidence
compatibility failures; those were resolved before the final passing command.
These are isolated code/fixture tests, including transactional fixture data; no
live provider probe, canonical business scan, import, research or OOS run was
performed for this merge.

The [original full-pool acceptance](2026-09-06_pool_scan_6.0_acceptance.md) remains
historical evidence for ad7fb45: 2953 processed, zero execution failures/timeouts,
977 blocked assessments, 403.64 seconds. It is not a new full-pool acceptance of
the merged source. The original branch and tag retain that exact provenance.
